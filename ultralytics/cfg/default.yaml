# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Global configuration YAML with settings and hyperparameters for YOLO training, validation, prediction and export
# For documentation see https://docs.ultralytics.com/usage/cfg/

task: detect # (str) YOLO task, i.e. detect, segment, classify, pose, obb, semantic
mode: train # (str) YOLO mode, i.e. train, val, predict, export, track, benchmark

# Train settings -------------------------------------------------------------------------------------------------------
model: # (str, optional) path to model file, i.e. yolov8n.pt or yolov8n.yaml
data: # (str, optional) path to data file, i.e. coco8.yaml
epochs: 100 # (int) number of epochs to train for
time: # (float, optional) max hours to train; overrides epochs if set
patience: 100 # (int) early stop after N epochs without val improvement
batch: 16 # (int | float) batch size as int (e.g. 16), or float 0.0–1.0 for AutoBatch fraction of GPU memory
val_batch: # (int, optional) validation batch size during training; empty uses the default train batch x2
val_workers: # (int, optional) validation dataloader workers; empty uses workers x2
imgsz: 640 # (int | list) image size; train/val may use int square or [h,w] rectangular
save: True # (bool) save train checkpoints and predict results
save_period: -1 # (int) save checkpoint every N epochs; disabled if < 1
cache: False # (bool | str) cache images in RAM (True/'ram') or on 'disk' to speed dataloading; False disables
device: # (int | str | list) device: 0 or [0,1,2,3] for CUDA, 'cpu'/'mps', or -1/[-1,-1] to auto-select idle GPUs
workers: 8 # (int) dataloader workers (per RANK if DDP)
project: # (str, optional) project name for results root
name: # (str, optional) experiment name; results in 'project/name'
exist_ok: False # (bool) overwrite existing 'project/name' if True
pretrained: True # (bool | str) use pretrained weights (bool) or load weights from path (str)
optimizer: auto # (str) optimizer: SGD, MuSGD, Prodigy, Adam, Adamax, AdamW, NAdam, RAdam, RMSProp, or auto
verbose: True # (bool) print verbose logs during training/val
seed: 0 # (int) random seed for reproducibility
deterministic: True # (bool) enable deterministic ops; reproducible but may be slower
single_cls: False # (bool) treat all classes as a single class
rect: False # (bool) rectangular batches for train; rectangular batching for val when mode='val'
cos_lr: False # (bool) cosine learning rate scheduler
close_mosaic: 10 # (int) disable mosaic augmentation for final N epochs (0 to keep enabled)
resume: False # (bool) resume training from last checkpoint in the run dir
amp: True # (bool) Automatic Mixed Precision (AMP) training; True runs AMP capability check
fraction: 1.0 # (float) fraction of training dataset to use (1.0 = all)
sampling: sequential # (str) train sampler: sequential/shuffle default, or weighted_random_with_replacement
samples_per_epoch: # (int, optional) number of replacement-sampled images per train epoch
sampling_weights: {} # (dict) source-name weights for weighted_random_with_replacement, matched against image path segments
class_aware_sampling: False # (bool) apply within-source rare-class reweighting after source-weighted sampling
class_aware_source: objects365 # (str) source name for class-aware reweighting
class_aware_power: 0.5 # (float) inverse class-frequency exponent; 0.5 is sqrt reweighting
class_aware_min_multiplier: 0.5 # (float) lower clamp for per-image class-aware multiplier before source normalization
class_aware_max_multiplier: 8.0 # (float) upper clamp for per-image class-aware multiplier before source normalization
small_object_sampling: False # (bool) boost images containing small boxes within one sampler source
small_object_source: objects365 # (str) source name used for small-object image reweighting
small_object_area: 1024.0 # (float) max pixel area for small-object sampler/crop targets, default 32^2
small_object_boost: 1.0 # (float) raw multiplier for small-object images before source normalization
small_object_crop: 0.0 # (float) probability of small-object-centric crop during training
small_object_crop_source: objects365 # (str) source name/path marker eligible for small-object crop
small_object_crop_area: 1024.0 # (float) max pixel area for choosing crop-centered small boxes
small_object_crop_scale: 0.72 # (float) crop window scale relative to current image before resizing back
small_object_crop_min_keep: 1 # (int) minimum kept boxes required to accept the crop
hard_image_list: # (str, optional) path or comma-separated image stems/paths to boost in weighted replacement sampling
hard_image_boost: 1.0 # (float) raw sampler boost for images in hard_image_list; <=1 disables
tal_topk_one2many: 10 # (int) YOLO26-PS one-to-many TaskAlignedAssigner candidates per GT
tal_topk_one2one: 7 # (int) YOLO26-PS one-to-one TaskAlignedAssigner pre-filter candidates per GT
tal_topk2_one2one: 1 # (int) YOLO26-PS one-to-one final TaskAlignedAssigner candidates per GT
tal_high_gt_threshold: 0 # (int) enable high-GT TAL guard above this total GT count per batch; 0 disables
tal_high_gt_topk_one2many: # (int, optional) one-to-many TAL top-k when high-GT guard is active
tal_high_gt_topk_one2one: # (int, optional) one-to-one TAL top-k when high-GT guard is active
tal_high_gt_topk2_one2one: # (int, optional) one-to-one final TAL top-k when high-GT guard is active
tal_metric_chunk_gt: 0 # (int) chunk GT boxes during TAL metric computation to reduce peak memory; 0 disables
pose_anchor_topk: 9 # (int) extra bbox-centered anchors per pose GT for denser keypoint supervision; 0 disables
pose_anchor_radius: 0.35 # (float) normalized bbox-center radius for extra pose anchors; <=0 uses the full bbox
pose_xy_loss: bbox # (str) YOLO26-PS 2D pose xy loss scale: bbox, grid, pixel, mpjpe, or oks/yolo
pose_xy_beta: 0.05 # (float) SmoothL1 beta for 2D pose xy loss in the selected scale
pose_oks_conf_weight: True # (bool) weight OKS keypoint loss by dataset visibility/confidence when available
pose_oks_max_px: 0.0 # (float) ignore visible OKS keypoints above this pixel error; <=0 disables
pose_oks_max_box_frac: 0.0 # (float) ignore visible OKS keypoints above this fraction of bbox height; <=0 disables
pose_oks_loss_clip: 1.0 # (float) clamp per-keypoint OKS loss before weighting; <=0 disables
pose_mpjpe_hard_px: 0.0 # (float) MPJPE hard-keypoint threshold in pixels; <=0 disables hard weighting
pose_mpjpe_hard_gain: 0.0 # (float) extra MPJPE loss weight gain for keypoints above pose_mpjpe_hard_px
pose_mpjpe_hard_power: 1.0 # (float) exponent for MPJPE hard-keypoint excess weighting
pose_mpjpe_hard_max: 4.0 # (float) max hard-keypoint weight before mean normalization; <=0 disables clamp
pose_mpjpe_kpt_weights: # (str, optional) comma-separated per-keypoint MPJPE weights; empty disables
pose_adapter: False # (bool) enable a residual feature adapter used only by the YOLO26-PS pose head
pose_adapter_hidden_ratio: 0.5 # (float) hidden channel ratio for the pose-only residual adapter
pose_adapter_scale: 1.0 # (float) residual scale for the pose-only adapter
det_class_mask_normalization: sqrt # (str) normalize partial-label detection BCE by supervised classes: sqrt, linear, or none
det_partial_cls_positive_only: False # (bool) only apply partial-label class BCE on assigned positives for single-class partial sources
det_area_loss_weight: False # (bool) apply sqrt(mean_area / area) weighting to assigned detection positives
det_area_loss_weight_max: 2.0 # (float) max clamp for det_area_loss_weight
det_nwd_ratio: 0.0 # (float) mix Normalized Wasserstein Distance into detection box loss, e.g. 0.3 for small objects
det_nwd_constant: 12.8 # (float) NWD exponential normalization constant in feature-grid box units
profile: False # (bool) profile ONNX/TensorRT speeds during training for loggers
train_profile_steps: 0 # (int) log average train batch stage timings for the first N batches; 0 disables
freeze: # (int | list, optional) freeze first N layers (int) or specific layer indices (list)
multi_scale: 0.0 # (float) multi-scale range as a fraction of imgsz; sizes are rounded to stride multiples
compile: False # (bool | str) enable torch.compile() backend='inductor'; True="default", False=off, or "default|reduce-overhead|max-autotune-no-cudagraphs"

# Segmentation
overlap_mask: True # (bool) merge instance masks into one mask during training (segment only)
mask_ratio: 4 # (int) mask downsample ratio (segment only)

# Classification
dropout: 0.0 # (float) dropout for classification head (classify only)

# Val/Test settings ----------------------------------------------------------------------------------------------------
val: True # (bool) run validation/testing during training
val_samples: # (int, optional) maximum number of validation images to evaluate; empty uses the full validation split
split: val # (str) dataset split to evaluate: 'val', 'test' or 'train'
save_json: False # (bool) save results to COCO JSON (detect/segment/pose) or PNG masks (semantic) for external evaluation
conf: # (float, optional) confidence threshold; defaults: predict=0.25, val=0.001
iou: 0.7 # (float) IoU threshold used for NMS
max_det: 300 # (int) maximum number of detections per image
half: False # (bool) use half precision (FP16) if supported
dnn: False # (bool) use OpenCV DNN for ONNX inference
plots: True # (bool) save plots and images during train/val
end2end: # (bool, optional) whether to use end2end head (YOLO26, YOLOv10) for predict/val/export

# Predict settings -----------------------------------------------------------------------------------------------------
source: # (str, optional) path/dir/URL/stream for images or videos; e.g. 'ultralytics/assets' or '0' for webcam
vid_stride: 1 # (int) read every Nth frame for video sources
stream_buffer: False # (bool) True buffers all frames; False keeps the most recent frame for low-latency streams
visualize: False # (bool) visualize model features (predict) or TP/FP/FN confusion (val)
augment: False # (bool) apply test-time augmentation during prediction
agnostic_nms: False # (bool) class-agnostic NMS
classes: # (int | list[int], optional) filter by class id(s), e.g. 0 or [0,2,3]
retina_masks: False # (bool) use high-resolution segmentation masks (segment)
embed: # (list[int], optional) return feature embeddings from given layer indices

# Visualize settings ---------------------------------------------------------------------------------------------------
show: False # (bool) show images/videos in a window if supported
save_frames: False # (bool) save individual frames from video predictions
save_txt: False # (bool) save results as .txt files (xywh format)
save_conf: False # (bool) save confidence scores with results
save_crop: False # (bool) save cropped prediction regions to files
show_labels: True # (bool) draw class labels on images, e.g. 'person'
show_conf: True # (bool) draw confidence values on images, e.g. '0.99'
show_boxes: True # (bool) draw bounding boxes on images
line_width: # (int, optional) line width of boxes; auto-scales with image size if not set

# Export settings ------------------------------------------------------------------------------------------------------
format: torchscript # (str) target format, e.g. torchscript|onnx|openvino|engine|coreml|saved_model|pb|tflite|edgetpu|tfjs|paddle|mnn|ncnn|imx|rknn|executorch|axelera|deepx
keras: False # (bool) TF SavedModel only (format=saved_model); enable Keras layers during export
optimize: False # (bool) TorchScript: mobile optimizations; DeepX: higher compiler optimization (slower compile, faster inference)
int8: False # (bool) INT8/PTQ where supported (openvino, engine, coreml, tflite, tfjs, mnn, imx, axelera, deepx); needs calibration data/fraction
dynamic: False # (bool) dynamic shapes for torchscript, onnx, openvino, engine, coreml; enable variable image sizes
simplify: True # (bool) ONNX/engine only; run graph simplifier for cleaner ONNX before runtime conversion
opset: # (int, optional) ONNX/engine only; opset version for export; leave unset to use a tested default
workspace: # (float, optional) engine (TensorRT) only; workspace size in GiB, e.g. 4
nms: False # (bool) fuse NMS into exported model when backend supports; if True, conf/iou apply (agnostic_nms except coreml)

# Hyperparameters ------------------------------------------------------------------------------------------------------
lr0: 0.01 # (float) initial learning rate (SGD=1e-2, Adam/AdamW=1e-3)
lrf: 0.01 # (float) final LR fraction; final LR = lr0 * lrf
momentum: 0.937 # (float) SGD momentum or Adam beta1
weight_decay: 0.0005 # (float) weight decay (L2 regularization)
prodigy_d0: 0.000001 # (float) Prodigy initial D estimate
prodigy_d_coef: 1.0 # (float) Prodigy D-estimate coefficient
prodigy_growth_rate: 0.0 # (float) Prodigy max D growth rate per step; 0 disables the cap
prodigy_slice_p: 1 # (int) Prodigy adaptation-stat slice stride; higher saves memory
prodigy_decouple: True # (bool) Prodigy decoupled AdamW-style weight decay
prodigy_use_bias_correction: False # (bool) Prodigy Adam bias correction
prodigy_safeguard_warmup: False # (bool) Prodigy warmup safeguard
warmup_epochs: 3.0 # (float) warmup epochs (fractions allowed)
warmup_momentum: 0.8 # (float) initial momentum during warmup
warmup_bias_lr: 0.1 # (float) bias learning rate during warmup
box: 7.5 # (float) box loss gain
cls: 0.5 # (float) classification loss gain
cls_pw: 0.0 # (float) class weights power for handling class imbalance (0.0=disable, 1.0=full inverse frequency)
det_focal_gamma: 0.0 # (float) detection focal gamma; 0 disables focal modulation
det_focal_alpha: 0.25 # (float) detection focal alpha; set <=0 to disable alpha balancing
dfl: 1.5 # (float) distribution focal loss gain
pose: 12.0 # (float) pose loss gain (pose tasks)
kobj: 1.0 # (float) keypoint objectness loss gain (pose tasks)
rle: 1.0 # (float) rle loss gain (pose tasks)
angle: 1.0 # (float) oriented angle loss gain (obb tasks)
nbs: 64 # (int) nominal batch size used for loss normalization
hsv_h: 0.015 # (float) HSV hue augmentation fraction
hsv_s: 0.7 # (float) HSV saturation augmentation fraction
hsv_v: 0.4 # (float) HSV value (brightness) augmentation fraction
degrees: 0.0 # (float) rotation degrees (+/-)
translate: 0.1 # (float) translation fraction (+/-)
scale: 0.5 # (float | tuple) scale gain (+/-) as float, or explicit (min, max) tuple
shear: 0.0 # (float) shear degrees (+/-)
perspective: 0.0 # (float) perspective fraction (0–0.001 typical)
flipud: 0.0 # (float) vertical flip probability
fliplr: 0.5 # (float) horizontal flip probability
bgr: 0.0 # (float) RGB↔BGR channel swap probability
mosaic: 1.0 # (float) mosaic augmentation probability
mixup: 0.0 # (float) MixUp augmentation probability
cutmix: 0.0 # (float) CutMix augmentation probability
copy_paste: 0.0 # (float) segmentation copy-paste probability
copy_paste_mode: flip # (str) copy-paste strategy for segmentation: flip or mixup
auto_augment: randaugment # (str) classification auto augmentation policy: randaugment, autoaugment, augmix
erasing: 0.4 # (float) random erasing probability for classification (0.0–1.0)

# Custom config.yaml ---------------------------------------------------------------------------------------------------
cfg: # (str, optional) path to a config.yaml that overrides defaults

# Tracker settings ------------------------------------------------------------------------------------------------------
tracker: botsort.yaml # (str) tracker config file: botsort.yaml or bytetrack.yaml
